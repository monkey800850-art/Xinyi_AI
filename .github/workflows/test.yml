name: test

on:
  push:
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    env:
      APP_ENV: test
      DATABASE_URL: sqlite:///test.db
      SECRET_KEY: ci-test-secret-key
      AUTH_ENABLE_RBAC: "0"
      PYTHONPATH: .
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ensure gate scripts executable
        run: |
          chmod +x scripts/ops/gate_consolidation.sh
          chmod +x scripts/ops/gate_release.sh

      - name: Run gate_consolidation.sh
        run: ./scripts/ops/gate_consolidation.sh

      - name: Run gate_release.sh
        run: ./scripts/ops/gate_release.sh

      - name: Test collect
        run: pytest -q --collect-only
