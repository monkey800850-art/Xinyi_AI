#!/usr/bin/env bash
set -euo pipefail
echo "[DEPRECATED] This repo uses ops-safety-kit/.githooks via core.hooksPath." >&2
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT"
bash ops-safety-kit/scripts/gate_all.sh
