# STEP25 UAT 执行结果报告（第一批可商用版本）

## 1. 文档信息

* **项目名称**：心易智能财务软件（Xinyi_AI）
* **阶段**：STEP25（第一批联调与完整测试回归 / UAT）
* **文档类型**：UAT 执行结果报告
* **执行环境**：Win11 + WSL2 Ubuntu-22.04
* **技术栈**：Python + Flask + MySQL + Alembic；Flask templates + static + 原生 JS/CSS
* **执行方式**：Codex CLI 主执行 + 人工复核；后续由 Codex Web 做体检复审
* **报告日期**：YYYY-MM-DD
* **时区**：Asia/Singapore

---

## 2. 版本快照（必须留档）

* **Git 分支**：`main`（或：`<your-branch>`）
* **最新 Commit ID**：`<commit_id>`
* **Commit Message**：`<commit_message>`
* **GitHub 仓库**：`monkey800850-art/Xinyi_AI`
* **Alembic 当前版本**：`<alembic_current_revision>`
* **数据库环境**：`本地测试库 / 测试环境库（脱敏）`
* **启动命令**：

  ```bash
  cd /home/x1560/Xinyi_AI
  python3 app.py
  ```
* **访问地址**：`http://127.0.0.1:5000`（如不同请填写实际地址）

---

## 3. 本轮 UAT 范围（纳入 / 不纳入）

### 3.1 本轮纳入范围（In Scope）

* [x] 系统管理闭环（用户 / 角色 / 规则 / 审计查询）
* [x] 固定资产类别管理（STEP22A）
* [x] 固定资产卡片管理（STEP22B）
* [x] 月度折旧计提（STEP22C，含防重复）
* [x] 资产变动（STEP22D：处置 / 报废 / 转移基础版）
* [x] 资产台账与折旧报表（STEP22E）
* [x] 跨模块冒烟回归（总账 / 付款 / 报销 / 银行 / 税务中的部分页面）

### 3.2 本轮不纳入范围（Out of Scope）

> 以下功能不在本轮 UAT 验收范围内，不作为失败项统计依据（如实际有请调整）

* [ ] 完整权限模型重构（当前仍依赖 `X-User` / `X-Role` 请求头机制）
* [ ] 高级审批流（资产审批、规则审批等）
* [ ] 自动化测试体系建设（pytest 全覆盖等）
* [ ] 全量历史数据迁移验证
* [ ] 性能压测 / 并发压测
* [ ] 生产部署与运维脚本验证

---

## 4. 测试数据准备（UAT 样本数据）

> 建议填写真实使用的 UAT 样本，以便复测与复盘。

### 4.1 系统管理样本

* **管理员用户**：`<admin_user>`
* **普通用户**：`<normal_user>`
* **角色样本**：

  * `UAT_ROLE_ADMIN`
  * `UAT_ROLE_FINANCE`
* **规则样本**（或菜单/权限规则）：

  * `UAT_RULE_SYSTEM_VIEW`
  * `UAT_RULE_ASSET_EDIT`

### 4.2 固定资产样本

#### 资产类别

* `UAT_CAT_OFFICE`（UAT办公设备）
* `UAT_CAT_VEHICLE`（UAT运输设备）

#### 资产卡片

* `UAT_FA_001`（在用、可折旧）
* `UAT_FA_002`（在用、可折旧）
* `UAT_FA_003`（特殊状态样本：闲置 / 待处置 / 其他）

#### 折旧 / 变动样本（如有）

* 历史折旧记录：`<有 / 无>`
* 历史资产变动记录：`<有 / 无>`

---

## 5. UAT 执行总览（结论）

### 5.1 总体结论（必填）

* **UAT总体结论**：`通过 / 有条件通过 / 不通过`
* **是否建议进入 STEP26**：`是 / 有条件是 / 否`

### 5.2 结论依据（建议 3~10 条）

1. `<示例：系统管理模块用户/角色/规则/审计四项页面与API均可访问并完成最小闭环>`
2. `<示例：固定资产类别、卡片、折旧、变动、台账/报表主路径已跑通>`
3. `<示例：STEP22C 月度折旧计提重复执行未产生重复记录（防重复有效）>`
4. `<示例：资产报废/处置后在后续折旧计提中被正确排除>`
5. `<示例：资产台账、折旧明细导出可用（CSV/Excel）>`
6. `<示例：发现P1问题若干，不阻断STEP26，已记录缺陷与后续处理建议>`

---

## 6. UAT 场景执行结果明细（S1~S7）

> 说明：状态统一使用 `PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`

---

### S1 系统管理闭环（UAT阻断项回归）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`
* **页面入口（实际路径）**：

  * 用户管理：`<path>`
  * 角色管理：`<path>`
  * 规则管理：`<path>`
  * 审计查询：`<path>`
* **关键 API（实际路径）**：

  * 用户列表：`<api>`
  * 用户保存：`<api>`
  * 角色列表/保存：`<api>`
  * 规则列表/保存：`<api>`
  * 审计查询：`<api>`

#### 执行摘要

* [ ] 用户列表查询成功
* [ ] 新增用户成功
* [ ] 编辑用户成功
* [ ] 角色新增/编辑成功
* [ ] 规则新增/编辑成功
* [ ] 审计查询能查询到操作记录（如设计支持）
* [ ] 无致命 JS 报错 / 无静态资源 404

#### 证据要点（路径/API/日志摘要）

* `<示例：GET /system/users 页面返回200，控制台无Uncaught错误>`
* `<示例：POST /api/system/users/save 返回 {"success": true, ...}>`
* `<示例：GET /api/system/audit/logs?keyword=UAT_USER_01 返回1条记录>`

#### 备注 / 风险

* `<示例：无请求头时 API 返回401/业务错误，属已知风险，UAT阶段通过固定头规避>`

---

### S2 固定资产类别管理（STEP22A）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`
* **页面入口**：`<path>`
* **关键 API**：

  * 类别列表：`<api>`
  * 类别保存：`<api>`
  * （启停/删除如有）：`<api>`

#### 执行摘要

* [ ] 类别列表查询成功
* [ ] 新增类别 `UAT_CAT_OFFICE` 成功
* [ ] 新增类别 `UAT_CAT_VEHICLE` 成功
* [ ] 编辑类别成功
* [ ] 重复编码校验生效
* [ ] 残值率/使用月数等字段校验生效（如实现）

#### 证据要点

* `<示例：POST /api/assets/categories/save 成功返回 category_id>`
* `<示例：重复编码提交返回错误信息 "类别编码已存在">`

#### 备注

* `<如某字段当前未纳入页面，但后端已支持，可在此说明>`

---

### S3 固定资产卡片管理（STEP22B）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`
* **页面入口**：`<path>`
* **关键 API**：

  * 卡片列表：`<api>`
  * 卡片详情：`<api>`
  * 卡片保存：`<api>`

#### 执行摘要

* [ ] 新增 `UAT_FA_001` 成功（在用、可折旧）
* [ ] 新增 `UAT_FA_002` 成功（在用、可折旧）
* [ ] 新增 `UAT_FA_003` 成功（特殊状态样本）
* [ ] 编辑卡片成功
* [ ] 类别默认值带出正常（年限/残值率/折旧方法至少一项）
* [ ] 重复资产编码校验生效
* [ ] 按类别/状态/关键字筛选可用

#### 证据要点

* `<示例：保存后列表可见 UAT_FA_001>`
* `<示例：切换类别后 useful_life_months 自动带出36>`

#### 备注

* `<如某字段是后端兜底计算而非页面展示，请说明>`

---

### S4 月度折旧计提（STEP22C，含防重复）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`
* **测试期间（Period）**：`YYYY-MM`
* **页面入口 / 计提入口**：`<path>`
* **关键 API**：

  * 月度计提执行：`<api>`
  * 折旧明细查询：`<api>`

#### 执行摘要

* [ ] 第一次计提执行成功
* [ ] 返回摘要包含扫描数/成功数/跳过数/失败数（或等价结构）
* [ ] 指定期间折旧明细可查询
* [ ] 第二次重复执行未产生重复记录（防重复通过）
* [ ] 重复执行返回“跳过/重复”提示（或等价结果）

#### 关键结果记录（建议填写真实值）

* **第一次执行摘要**：

  * 扫描数：`<n>`
  * 成功数：`<n>`
  * 跳过数：`<n>`
  * 失败数：`<n>`
* **第二次执行摘要**：

  * 扫描数：`<n>`
  * 成功数：`0`（理想情况）
  * 跳过数：`<n>`
  * 失败数：`<n>`

#### 证据要点

* `<示例：POST /api/assets/depreciation/run?period=2026-02 第一次 success>`
* `<示例：第二次执行返回 skipped_reasons 包含 duplicate>`
* `<示例：查询明细记录条数未增加>`

#### 备注 / 风险

* `<如仅服务层防重复、DB无唯一约束，可在此标记为技术债/P2>`

---

### S5 资产变动（STEP22D：处置 / 报废 / 转移）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`
* **页面入口**：`<path>`
* **关键 API**：

  * 转移登记：`<api>`
  * 报废登记：`<api>`
  * 处置登记：`<api>`
  * 变动记录查询：`<api>`

#### 执行摘要

* [ ] 转移登记成功（资产归属字段联动更新）
* [ ] 报废登记成功（资产状态变更）
* [ ] 处置登记成功（资产状态变更）
* [ ] 变动记录可按类型/资产/日期查询
* [ ] 重复报废被拦截
* [ ] 重复处置被拦截
* [ ] 与折旧联动回归通过（已报废/已处置资产不再参与折旧）

#### 证据要点

* `<示例：POST /api/assets/change/transfer 成功后 asset.location 更新>`
* `<示例：POST /api/assets/change/scrap 成功后 status=SCRAPPED>`
* `<示例：S4回归计提时 UAT_FA_003 被跳过，原因=status_not_depreciable>`

#### 备注

* `<如“撤销变动”未实现，标注范围外或后续优化项>`

---

### S6 资产台账与折旧报表（STEP22E）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`
* **页面入口**：

  * 资产台账：`<path>`
  * 折旧报表：`<path>`
* **关键 API / 导出接口**：

  * 台账查询：`<api>`
  * 折旧明细查询：`<api>`
  * 汇总查询（如有）：`<api>`
  * 台账导出：`<api>`
  * 折旧明细导出：`<api>`

#### 执行摘要

* [ ] 资产台账按关键字查询可用
* [ ] 资产台账按类别/状态筛选可用
* [ ] 台账关键字段显示正确（编码/名称/类别/状态/原值/累计折旧/净值等）
* [ ] 折旧明细按期间查询可用
* [ ] 汇总统计可用（如实现）
* [ ] 资产台账导出成功
* [ ] 折旧明细导出成功

#### 证据要点

* `<示例：GET /api/assets/ledger?keyword=UAT_FA_001 返回1条>`
* `<示例：下载 assets_ledger_2026-02-27.xlsx 成功>`
* `<示例：折旧明细导出响应头 Content-Disposition 正常>`

#### 数据口径说明（务必填写）

* **净值计算口径**：`实时计算 / 存储字段 / 混合`
* **累计折旧来源**：`折旧记录汇总 / 资产主表字段 / 混合`
* **报表期间定义**：`按period字段 / 按日期范围折算 / 其他`

---

### S7 跨模块冒烟回归（总账 / 付款 / 报销 / 银行 / 税务）

* **状态**：`PASS / FAIL-P0 / FAIL-P1 / FAIL-P2 / BLOCKED`
* **前置条件是否满足**：`是 / 否`

#### 抽查模块与结果

| 模块 | 页面路径     | 操作        | 结果          | 备注     |
| -- | -------- | --------- | ----------- | ------ |
| 总账 | `<path>` | 页面打开/列表查询 | `PASS/FAIL` | `<备注>` |
| 付款 | `<path>` | 页面打开/列表查询 | `PASS/FAIL` | `<备注>` |
| 报销 | `<path>` | 页面打开/列表查询 | `PASS/FAIL` | `<备注>` |
| 银行 | `<path>` | 页面打开/列表查询 | `PASS/FAIL` | `<备注>` |
| 税务 | `<path>` | 页面打开/列表查询 | `PASS/FAIL` | `<备注>` |

#### 证据要点

* `<示例：各页面HTTP 200，控制台无新增致命错误>`
* `<示例：某模块出现404静态资源，已记录为P1回归风险>`

---

## 7. UAT 证据索引（便于复核）

> 可按实际情况填写文件路径、截图名、日志片段位置。若证据未入仓库，可写“本地留存”。

### 7.1 页面与截图证据

* `docs/uat/evidence/screenshots/s1_system_users.png`（或本地留存）
* `docs/uat/evidence/screenshots/s4_depreciation_run_result.png`
* `docs/uat/evidence/screenshots/s6_asset_ledger_export.png`

### 7.2 API / curl / Postman 证据

* `docs/uat/evidence/curl/s1_system_api_checks.txt`
* `docs/uat/evidence/curl/s4_depreciation_duplicate_check.txt`
* `docs/uat/evidence/curl/s6_reports_export_checks.txt`

### 7.3 服务端日志证据

* `docs/uat/evidence/logs/uat_run_YYYYMMDD.log`（或本地留存）
* 关键错误片段：`<时间 + 路径 + 摘要>`

---

## 8. 缺陷清单（P0 / P1 / P2）

> 若无缺陷，也请写“本轮未发现阻断性缺陷”，并保留空表结构。

### 8.1 P0（阻断 UAT / 阻断进入下一阶段）

| 缺陷ID      | 标题     | 所属场景 | 复现步骤   | 实际结果   | 预期结果   | 影响范围   | 建议处理   | 当前状态                         |
| --------- | ------ | ---- | ------ | ------ | ------ | ------ | ------ | ---------------------------- |
| `UAT-001` | `<标题>` | `S4` | `<步骤>` | `<实际>` | `<预期>` | `<影响>` | `立即修复` | `Open / Fixed / Retest-Pass` |

### 8.2 P1（高优先，可继续测）

| 缺陷ID      | 标题     | 所属场景 | 复现步骤   | 实际结果   | 预期结果   | 影响范围   | 建议处理                  | 当前状态                         |
| --------- | ------ | ---- | ------ | ------ | ------ | ------ | --------------------- | ---------------------------- |
| `UAT-101` | `<标题>` | `S1` | `<步骤>` | `<实际>` | `<预期>` | `<影响>` | `STEP25内修复或STEP26前修复` | `Open / Fixed / Retest-Pass` |

### 8.3 P2（可延期）

| 缺陷ID      | 标题     | 所属场景 | 复现步骤   | 实际结果   | 预期结果   | 影响范围   | 建议处理   | 当前状态                         |
| --------- | ------ | ---- | ------ | ------ | ------ | ------ | ------ | ---------------------------- |
| `UAT-201` | `<标题>` | `S6` | `<步骤>` | `<实际>` | `<预期>` | `<影响>` | `后续优化` | `Open / Fixed / Retest-Pass` |

---

## 9. 本轮修复记录（如有）

> 若 STEP25 执行中对缺陷进行了最小修复，请记录在这里，方便 Codex Web 体检与后续追踪。

### 9.1 已修复项（UAT中修复）

| 修复ID      | 对应缺陷ID    | 修复内容     | 变更文件            | Commit ID  | 回归验证结果                      |
| --------- | --------- | -------- | --------------- | ---------- | --------------------------- |
| `FIX-001` | `UAT-001` | `<修复内容>` | `<file1,file2>` | `<commit>` | `Retest-Pass / Retest-Fail` |

### 9.2 未修复项（保留到后续）

| 缺陷ID      | 等级   | 未修复原因  | 计划处理阶段                    |
| --------- | ---- | ------ | ------------------------- |
| `UAT-101` | `P1` | `<原因>` | `STEP26前 / STEP26 / 后续版本` |

---

## 10. UAT 阻断项复核结果（重点项）

> 这些是决定是否进入 STEP26 的关键复核项。

### 10.1 系统管理闭环（用户/角色/规则/审计）

* **状态**：`通过 / 部分通过 / 失败`
* **风险等级**：`低 / 中 / 高`
* **说明**：`<填写实际情况>`

### 10.2 权限请求头风险（X-User / X-Role）

* **状态**：`通过 / 部分通过 / 失败`
* **风险等级**：`低 / 中 / 高`
* **说明**：`<无请求头行为、UAT临时措施、后续优化建议>`

### 10.3 折旧防重复（STEP22C核心）

* **状态**：`通过 / 部分通过 / 失败`
* **风险等级**：`低 / 中 / 高`
* **说明**：`<服务层检查 / DB唯一约束 / 重复执行结果>`

### 10.4 资产变动与折旧联动（STEP22D↔STEP22C）

* **状态**：`通过 / 部分通过 / 失败`
* **风险等级**：`低 / 中 / 高`
* **说明**：`<报废/处置后是否被排除折旧>`

### 10.5 资产台账 / 折旧报表 / 导出可用性（STEP22E）

* **状态**：`通过 / 部分通过 / 失败`
* **风险等级**：`低 / 中 / 高`
* **说明**：`<查询/汇总/导出是否齐备，缺口是什么>`

---

## 11. 已知限制（进入下一步前需保留说明）

> 这里写“不是缺陷，但会影响使用/验收预期”的内容，方便交接。

1. `<示例：当前权限机制仍依赖 X-User / X-Role 请求头，尚未完成正式登录态权限贯通>`
2. `<示例：折旧防重复当前以服务层幂等检查为主，数据库唯一约束待补强>`
3. `<示例：某报表仅支持CSV导出，Excel导出计划后续补充>`
4. `<示例：部分审计筛选条件为基础版，复杂组合筛选未纳入本轮>`

---

## 12. 建议后续动作（按优先级）

### 12.1 立即处理（进入 STEP26 前建议完成）

1. `<如有P0/P1项，逐条填写>`
2. `<如无，可写：无阻断项，建议直接进入STEP26>`

### 12.2 STEP26 内处理（可并行）

1. `<P1/P2项>`
2. `<文档完善项>`
3. `<回归补测项>`

### 12.3 后续版本处理

1. `<性能/并发/权限体系重构等技术债>`
2. `<体验优化项>`

---

## 13. 是否建议进入 STEP26（最终判定）

* **判定结果**：`建议进入 / 有条件进入 / 暂不建议进入`
* **判定说明**：

  * `<示例：主业务路径已跑通，UAT结果可支撑进入STEP26；存在P1项X条，不阻断进入>`
  * `<示例：存在P0缺陷（UAT-001），建议修复并复测后再进入STEP26>`

---

## 14. 附录：常用验证命令（可选）

> 可把本轮关键 curl 命令放这里，方便复测与 Codex Web 复核。

### 14.1 系统管理（示例）

```bash
curl -s -H "X-User: admin" -H "X-Role: admin" "http://127.0.0.1:5000/api/system/users"
```

### 14.2 折旧计提（示例）

```bash
curl -s -X POST -H "X-User: admin" -H "X-Role: admin" \
  "http://127.0.0.1:5000/api/assets/depreciation/run?period=2026-02"
```

### 14.3 折旧明细查询（示例）

```bash
curl -s -H "X-User: admin" -H "X-Role: admin" \
  "http://127.0.0.1:5000/api/assets/depreciation/records?period=2026-02"
```

### 14.4 资产台账查询（示例）

```bash
curl -s -H "X-User: admin" -H "X-Role: admin" \
  "http://127.0.0.1:5000/api/assets/ledger?keyword=UAT_FA_001"
```

---

## 15. 备注（可选）

* `<填写本轮执行中的特殊情况，例如服务重启、数据库重置、临时绕过方式等>`
* `<填写与上个窗口 TASK CARD 25 的对应关系：本文件为 STEP25 的执行结果落地文档>`
